{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.apvero.dev/release-bundle-manifest/1.1.0",
  "title": "Apvero Release Bundle Manifest 1.1",
  "description": "Strict immutable release contract implemented for CHAT and grounded RAG applications.",
  "x-apvero-contract-status": "baseline",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "runtimeMode",
    "modelRouteVersion",
    "promptVersion",
    "outputSchemaVersion",
    "knowledgeBindings",
    "capabilityVersions",
    "policyVersions",
    "memoryPolicyVersion",
    "evaluationReportVersion",
    "runtimeParameters"
  ],
  "properties": {
    "schemaVersion": { "const": "1.1" },
    "runtimeMode": { "enum": ["CHAT", "RAG"] },
    "modelRouteVersion": { "$ref": "#/$defs/integerVersionReference" },
    "promptVersion": { "$ref": "#/$defs/integerVersionReference" },
    "outputSchemaVersion": { "$ref": "#/$defs/exactArtifactReference" },
    "knowledgeBindings": {
      "type": "array",
      "maxItems": 16,
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/knowledgeBinding" }
    },
    "capabilityVersions": {
      "type": "array",
      "maxItems": 64,
      "items": { "$ref": "#/$defs/exactArtifactReference" },
      "uniqueItems": true
    },
    "policyVersions": {
      "type": "array",
      "minItems": 1,
      "maxItems": 64,
      "items": { "$ref": "#/$defs/exactArtifactReference" },
      "uniqueItems": true
    },
    "memoryPolicyVersion": { "$ref": "#/$defs/exactArtifactReference" },
    "evaluationReportVersion": { "$ref": "#/$defs/exactArtifactReference" },
    "runtimeParameters": {
      "type": "object",
      "additionalProperties": false,
      "required": ["temperature", "maxOutputTokens"],
      "properties": {
        "temperature": { "type": "number", "minimum": 0, "maximum": 2 },
        "maxOutputTokens": { "type": "integer", "minimum": 1, "maximum": 200000 }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "runtimeMode": { "const": "CHAT" } },
        "required": ["runtimeMode"]
      },
      "then": {
        "properties": { "knowledgeBindings": { "maxItems": 0 } }
      }
    },
    {
      "if": {
        "properties": { "runtimeMode": { "const": "RAG" } },
        "required": ["runtimeMode"]
      },
      "then": {
        "properties": { "knowledgeBindings": { "minItems": 1 } }
      }
    }
  ],
  "$defs": {
    "integerVersionReference": {
      "type": "string",
      "minLength": 3,
      "maxLength": 240,
      "description": "Exact identity used by the implemented Model Route and Prompt aggregates.",
      "pattern": "^(?!.*(?:^|[@:/-])latest(?:$|[@:/-]))[a-z0-9][a-z0-9._:/-]*@[1-9][0-9]*$"
    },
    "exactArtifactReference": {
      "type": "string",
      "minLength": 3,
      "maxLength": 240,
      "description": "Exact integer- or semantic-versioned artifact identity; latest is forbidden.",
      "pattern": "^(?!.*(?:^|[@:/-])latest(?:$|[@:/-]))[a-z0-9][a-z0-9._:/-]*@(?:[1-9][0-9]*|[0-9]+\\.[0-9]+\\.[0-9]+(?:-[a-z0-9.-]+)?)$"
    },
    "semanticVersionReference": {
      "type": "string",
      "minLength": 7,
      "maxLength": 240,
      "pattern": "^(?!.*(?:^|[@:/-])latest(?:$|[@:/-]))[a-z0-9][a-z0-9._:/-]*@[0-9]+\\.[0-9]+\\.[0-9]+(?:-[a-z0-9.-]+)?$"
    },
    "knowledgeBinding": {
      "type": "object",
      "additionalProperties": false,
      "required": ["indexVersion", "retrievalPolicyVersion"],
      "properties": {
        "indexVersion": { "$ref": "#/$defs/semanticVersionReference" },
        "retrievalPolicyVersion": { "$ref": "#/$defs/semanticVersionReference" }
      }
    }
  }
}
